{
  "name": "home",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prerender": "node scripts/prerender.mjs",
    "dev": "npm run prerender && vite",
    "build": "npm run prerender && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "playwright test -c tools/browser/playwright.config.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.2",
    "dompurify": "^3.4.10",
    "haunted": "^6.1.0",
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "@lit-labs/ssr": "^4.1.0",
    "@playwright/test": "1.50.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vitest": "^4.1.9"
  }
}
